# Setup schema
# create schema if not exists security;

# create user 'security-user'@'localhost'
#     identified by 'password1';

# GRANT ALL PRIVILEGES ON security . * TO 'security-user';

# insert into security.role (id, name) value (1, 'USER');

